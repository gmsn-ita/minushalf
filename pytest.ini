[pytest]
filterwarnings=
	# workaround for https://github.com/pypa/setuptools/issues/479
	ignore:the imp module is deprecated in favour of importlib:DeprecationWarning

testpaths =
      minushalf
norecursedirs= 
	    dist build .tox minushalf
